<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_graph"
    app:startDestination="@id/splash_flow">

    <include app:graph="@navigation/splash_flow" />

    <include app:graph="@navigation/welcome_flow" />

    <action
        android:id="@+id/action_welcome_flow"
        app:destination="@id/welcome_flow"
        app:popUpTo="@id/root_graph" />

    <!-- <fragment
         android:id="@+id/splashFragment"
         android:name="od.konstantin.krok.ui.splash.SplashFragment"
         android:label="SplashFragment">

         <action
             android:id="@+id/action_splash_to_welcome"
             app:destination="@id/welcome_fragment"
             app:popUpTo="@id/splashFragment"
             app:popUpToInclusive="true" />
     </fragment>

     <fragment
         android:id="@+id/welcome_fragment"
         android:name="od.konstantin.feature_welcome.ui.WelcomeFragment"
         android:label="WelcomeFragment" />-->
</navigation>